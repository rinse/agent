# コードレビュー観点

## 基本方針

- レビューコメントはすべて **日本語** で記述する
- コードへの指摘は GitHub の **インラインコメント** （PR の diff ビューで対象行を選択して投稿するコメント）で行う
  - 指摘対象のコード範囲を diff 上で **複数行選択** してからコメントを書く
  - コメント本文にファイルパスや行番号を書く必要はない（GitHub が自動的にコードとコメントを紐付ける）

## レビュー観点

### 1. 正確性

- ロジックに誤りがないか（off-by-one、境界条件、パターンマッチの網羅性）
- `unwrap()` / `expect()` の使用が安全であることが自明か。不明な場合は `Result` で伝搬しているか
- 非同期処理でデッドロックやレースコンディションの可能性がないか

### 2. 設計・構造

- クレート間の依存方向が ports → agent-core → adapters → cli の順を守っているか
- トレイト境界が必要最小限か。不要な `Clone` / `'static` を要求していないか
- 公開 API (`pub`) のスコープが適切か。内部実装が漏れていないか

### 3. エラーハンドリング

- エラー型が呼び出し元にとって有用な情報を持っているか
- エラーの握り潰し（`let _ = ...`、空の `catch`）がないか
- パニックする可能性のあるコードがライブラリクレートに含まれていないか

### 4. 安全性・セキュリティ

- `unsafe` ブロックの使用に正当な理由と安全性の根拠があるか
- 外部入力（JSON、ユーザー入力）のバリデーションが境界で行われているか
- 機密情報（APIキー、トークン）がログや出力に漏れていないか

### 5. パフォーマンス

- 不要なクローンやアロケーションがないか（`&str` で済む場面で `String` を渡していないか）
- `async` 関数内でブロッキング I/O を呼んでいないか
- 大きなデータ構造のコピーが発生していないか

### 6. テスト

- 追加・変更したロジックに対応するテストがあるか
- テストが実装の詳細ではなく振る舞いを検証しているか
- エッジケース（空入力、最大値、エラーパス）がカバーされているか

### 7. 可読性・命名

- 変数名・関数名が意図を正確に表しているか
- 不要なコメントがないか（コードで自明なことを繰り返していないか）
- 関数の長さが適切か。1つの関数が複数の責務を持っていないか

### 8. 依存関係

- 新しい外部クレートの追加に正当な理由があるか
- `workspace.dependencies` で バージョンを一元管理しているか
- ライセンスの互換性に問題がないか

## インラインコメントの書き方

### 手順

1. PR の **Files changed** タブを開く
2. 指摘したいコードの **開始行の行番号をクリック** し、**終了行まで Shift+クリック** で範囲を選択する
3. 表示されるコメント欄に指摘を記入する
4. 個別のコメントは **Start a review** でレビューに蓄積し、全ての指摘を書き終えてから **Submit review** でまとめて投稿する

### 重要度の接頭辞

コメント本文の先頭に以下の接頭辞を付けて重要度を示す:

| 接頭辞 | 意味 | 対応 |
|---|---|---|
| **[必須]** | マージをブロックする問題 | 修正が必要 |
| **[推奨]** | 改善した方がよい点 | 対応を検討 |
| **[nit]** | 些細な指摘（命名、フォーマット等） | 任意 |
| **[質問]** | 意図や背景の確認 | 回答が必要 |

### コメント例

以下はインラインコメントの **本文** として記述する内容の例。
ファイルパスや行番号はコメントの配置位置で自明なため、本文には含めない。

> **[必須]**
> この `unwrap()` は外部入力由来の値に対して呼ばれており、不正な入力でパニックする。
> `map_err` で適切なエラー型に変換して `?` で伝搬すべき。

> **[推奨]**
> この構造体は `Clone` を derive しているが、呼び出し側で実際に `Clone` が必要な箇所がない。不要であれば削除を検討。

> **[nit]**
> `process_data` より `parse_tool_output` の方が具体的で意図が伝わりやすい。

> **[質問]**
> ここで履歴を切り詰めている理由は何か？ コンテキストウィンドウの制約対応であれば、上限値を定数として切り出した方が変更しやすいのでは。
